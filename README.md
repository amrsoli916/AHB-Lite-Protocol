# AHB-Lite-Protocol
This project implements a configurable AMBA AHB-Lite Master in SystemVerilog. The design supports single and burst transfers, read/write operations, address generation, burst counting, timeout detection, and an FSM-based controller compliant with the AMBA AHB-Lite protocol.
